---
interface Props {
    marginTop?: string;
    marginBottom?: string;
}

const { marginTop = "mt-10", marginBottom = "mb-8" } = Astro.props;
---

<div 
    class={`${marginTop} ${marginBottom} h-px w-full bg-linear-to-r from-transparent via-gray-300 to-transparent dark:via-gray-800`}>
</div>